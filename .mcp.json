{
  "mcpServers": {
    "bugs": {
      "command": "node",
      "args": ["./mcp/dist/index.js"],
      "env": {
        "TRACKER_URL": "${TRACKER_URL:-http://127.0.0.1:4310}",
        "TRACKER_TOKEN": "${TODONT_TOKEN}"
      }
    }
  }
}
